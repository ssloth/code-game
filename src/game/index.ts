import Phaser from 'phaser';
import MainScene from './main';
import TextScene from './test';

// export default ([MainScene] as unknown) as Phaser.Scene[];
export default ([TextScene] as unknown) as Phaser.Scene[];
